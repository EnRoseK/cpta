'use strict';

/**
 * translations-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::translations-page.translations-page');
