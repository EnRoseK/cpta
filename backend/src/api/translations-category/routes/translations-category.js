'use strict';

/**
 * translations-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::translations-category.translations-category');
